---
  - name: Show IOS version of devices
    hosts: all

    tasks:
      - name: Execute "show version" command
        cisco.ios.ios_command:
          commands:
            - command: show version
        register: result

      - name: Print result
        debug: var=result.stdout_lines[0]