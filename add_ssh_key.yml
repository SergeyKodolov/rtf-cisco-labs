---

  - name: Adding server key
    hosts: routers
  
    tasks:
      - name: Send Ansible server key
        cisco.ios.ios_user:
          name: ansible
          sshkey: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
          state: present